spring:
  application:
    name: api-gateway-assignment

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

    openfeign:
      circuitbreaker:
        enabled: true

  datasource:
    url: jdbc:mysql://localhost:3306/api-gateway-security?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: K9254490344k@

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 9996

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:9997/eureka/

jwt:
  secret: "=======================Kunal=Spring==========================="
  expiration-ms: 1800000
